<?php include __DIR__ . '/../../includes/header.php'; require_permission('visitantes.ver'); $id=$_GET['id']??0; $st=$pdo->prepare("SELECT * FROM visitantes WHERE id_visitante=?");$st->execute([$id]);$v=$st->fetch(); ?>
<h1>Pase QR de visitante</h1><div class="card"><?php if($v): ?><h2><?= e($v['nombres']) ?></h2><p><strong>Documento:</strong> <?= e($v['documento']) ?></p><p><strong>Estado:</strong> <?= e($v['estado_autorizacion']) ?></p><div class="qrbox"><?= e($v['qr_generado']) ?></div><p class="footer-note">Copia este código y úsalo en Control de Accesos → Validar QR.</p><?php else: ?><p>No encontrado.</p><?php endif; ?><a class="btn btn-secondary" href="index.php">Volver</a></div><?php include __DIR__ . '/../../includes/footer.php'; ?>
